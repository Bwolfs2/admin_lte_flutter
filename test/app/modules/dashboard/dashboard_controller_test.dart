import 'package:admin_lte_flutter/app/modules/dashboard/dashboard_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(DashboardModule());
  // DashboardController dashboard;
  //
  setUp(() {
    //     dashboard = DashboardModule.to.get<DashboardController>();
  });

  group('DashboardController Test', () {
    //   test("First Test", () {
    //     expect(dashboard, isInstanceOf<DashboardController>());
    //   });

    //   test("Set Value", () {
    //     expect(dashboard.value, equals(0));
    //     dashboard.increment();
    //     expect(dashboard.value, equals(1));
    //   });
  });
}
