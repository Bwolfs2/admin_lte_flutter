import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:admin_lte_flutter/app/modules/my_projects/my_projects_page.dart';

main() {
  testWidgets('MyProjectsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MyProjectsPage(title: 'MyProjects')));
    //  final titleFinder = find.text('MyProjects');
    //  expect(titleFinder, findsOneWidget);
  });
}
