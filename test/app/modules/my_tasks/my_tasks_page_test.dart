import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:admin_lte_flutter/app/modules/my_tasks/my_tasks_page.dart';

main() {
  testWidgets('MyTasksPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MyTasksPage(title: 'MyTasks')));
    //  final titleFinder = find.text('MyTasks');
    //  expect(titleFinder, findsOneWidget);
  });
}
